###### ***חלק א': הכנת "ערכת ההתקנה" (בדיסק-און-קי) 💾***



אלו הקבצים שאת צריכה להעתיק מהמחשב שלך לדיסק-און-קי. הוספתי הסבר קצר על כל קובץ כדי שתדעי מה תפקידו:



**1. קבצי הקוד (הלב של המערכת)**

 main.py - "המוח". מנהל את פקודות האילוסטרייטור והלוגיקה.

 illustrator_ops.py - "הידיים". מכיל את הסקריפטים שמזיזים דברים בתוך אילוסטרייטור.

 gui.py - "הפנים". המסך היפה (Streamlit) שהעובד רואה.

 vectorizer_ops.py - "המרת הקבצים". הופך תמונות רגילות (PNG/JPG) לקבצי וקטור (SVG) כדי שיהיו איכותיים.

 server.js - "הגשר". השרת שמקשר בין האתר/מערכת ההזמנות לבין הקוד של הפייתון.

prepare_print.py - "מכין הדפוס". מסדר את הלוגו וההדמיה לפני שלב הפלט הסופי.


**2. קבצי ניהול ועדכון**

 run_auto.bat - "הכפתור הירוק". הקובץ היומיומי. מעדכן מה-GitHub ומפעיל את המערכת.

 Setup_System.bat - מריצים אותו פעם אחת במחשב חדש כדי להתקין את כל הספריות בבת אחת.

 run_me.py - "המעדכן". הסקריפט שבודק מול GitHub ומוריד קבצים חדשים אם יש.

 requirements.txt - רשימת הספריות שפייתון צריך.

 package.json - רשימת הקניות של האתר (Node.js).

 package_lock.json – רשימת הקניות של האתר (Node.js).

 config.json - "תעודת הזהות". הקובץ שבו מגדירים לאיזו תיקייה לשמור את הקבצים (משתנה ממחשב למחשב).

 Run_Dev_Mode.bat - "מצב פיתוח". מפעיל את השרת בלי לעדכן מהענן (כדי שלא יידרס קוד שעובדים עליו).


**3. תיקיות (חובה!)**

&nbsp;   📂 Simulations – מכילה את קבצי האילוסטרייטור (התבניות).


**❌ מה לא להעתיק? **

אל תעתיקי את התיקיות האלו, זה סתם מכביד והמחשב החדש ייצור אותן בעצמו:


❌ gui.py - ממשק גרפי ישן לבדיקות ידניות.

❌ Run_System.bat - מפעיל את ה-GUI.

❌ splitter.py / print_automation.py / photoshop_automation.py / simple_save_test.py / inspect_svg.py / debug.py - קבצים שנועדו רק לאתר תקלות בקוד.

❌ Install_Libraries.bat / install_server.bat - קבצי התקנה ישנים

❌ vector_cache - קובץ זיכרון.

❌ תיקיית venv (סביבת הפייתון).

❌ תיקיית node_modules (ספריות האתר).

❌ תיקיית __pycache__.

❌ תיקיית temp_downloads - תקיות זמניות.

❌ תיקיית temp_uploads - תקיות זמניות.



***חלק ב': הכנת המחשב החדש (לפני שמעבירים קבצים) 🛠️***

לפני שאת מחברת את הדיסק-און-קי, את צריכה להתקין 3 תוכנות בסיס במחשב החדש:

1- Python (גרסה 3.10.11).
   ⚠️ חשוב מאוד: במסך ההתקנה הראשון, סמני ב-V את הריבוע למטה: "Add Python to PATH".

2- Node.js (גרסה LTS - Long Term Support).

3- Git (כדי שהעדכונים האוטומטיים יעבדו).



***חלק ג': העתקת הקבצים והגדרות 📂***

1- צרי תיקייה חדשה במחשב (למשל: C:\\Auto_Print_System).

2- העתיקי לתוכה את כל הקבצים והתיקיות מהדיסק-און-קי (מ"חלק א").

3- יצירת קובץ הגדרות (config.json): פתחי את הקובץ ושני את הנתיבים כך שיתאימו למשתמש החדש (נתיב תיקיית השמירה).



***חלק ד': הפעלה ראשונית 🚀***

דאבל-קליק על Setup_System.bat. (חכי בסבלנות עד שייכתב "The system is ready").
זהו! המחשב מוכן.



מעכשיו, הגרפיקאי צריך ללחוץ רק על run_auto.bat. הקובץ הזה יבדוק אם יש עדכונים ב-GitHub, ימשוך אותם, ויפעיל את התוכנה.






ירדן פדלון.